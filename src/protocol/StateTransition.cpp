// Placeholder interface for future protocol state transition logic.
// This keeps CMake satisfied and allows the build to proceed.

class StateTransition {
public:
    // Minimal placeholder method
    bool apply() const;
};
